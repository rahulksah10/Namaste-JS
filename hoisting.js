getName()
console.log(a);
console.log(getName);

let a =7
function getName(){
    console.log("Namaste Rahul");
}

// getName()
// console.log(a);  if we execute this part only we will not gate any eror while excuting the same code in before initilizaing the function it will excuted properly but not for the variable , for variable it will shows error like  "Cannot access 'a' before initialization"  or  "undefined"
